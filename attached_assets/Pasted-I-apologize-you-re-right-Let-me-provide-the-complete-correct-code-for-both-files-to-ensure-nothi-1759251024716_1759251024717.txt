I apologize - you're right. Let me provide the complete, correct code for both files to ensure nothing breaks:

**Complete `client/src/components/workspace/WorkspaceShell.tsx`:**

At the top, add the import (around line 14, with the other imports):
```tsx
import { FloatingLayer } from './FloatingLayer';
```

Then at the very end of the component, the complete return statement should be:

```tsx
return (
  <div ref={workspaceRef} className="relative w-full min-h-screen bg-background">
    {isMobile ? (
      /* Mobile Layout - Full screen with drawer */
      <>
        {/* Full-width main content for mobile */}
        <div className="w-full min-h-screen">
          {children}
        </div>
        
        {/* Mobile Sidebar Drawer */}
        {dockedPanels.length > 0 && (
          <SidebarDrawer
            isOpen={isMobileDrawerOpen}
            onClose={closeMobileDrawer}
            title="Writing Tools"
          >
            {renderSidebarContent()}
          </SidebarDrawer>
        )}
      </>
    ) : (
      /* Desktop Layout - Side-by-side */
      <div className="flex w-full min-h-screen">
        {/* Main Content Area */}
        <div className="flex-1 min-w-0">
          {children}
        </div>
        
        {/* Desktop Docked Sidebar */}
        {dockedPanels.length > 0 && (
          <div 
            className="w-96 border-l border-border bg-background flex-shrink-0 self-start docked-sidebar-sticky"
            data-testid="docked-sidebar-container"
          >
            {renderSidebarContent()}
          </div>
        )}
      </div>
    )}

    {/* Floating Layer - renders globally for ALL pages */}
    <FloatingLayer />
  </div>
);
```

**Complete end of `client/src/components/workspace/WorkspaceLayout.tsx`:**

The return statement should end like this (keep everything else the same, just showing the end):

```tsx
      ) : (
        // Single region layout
        <div className="flex-1 flex flex-col">
          <div>
            <TabStrip 
              regionId="main" 
              onDrop={(e) => handleTabBarDrop(e, 'main')}
              onDragOver={handleTabBarDragOver}
              projectInfo={projectInfo}
            />
          </div>
          
          <div className="flex-1 overflow-hidden">
            {mainActiveTab ? (
              // Show active tab content as reference
              <div className="h-full bg-muted/20">
                {renderTabContent(mainActiveTab)}
              </div>
            ) : (
              // Always show primary manuscript content when no reference tabs are active
              children
            )}
          </div>
        </div>
      )}
    </div>
  );
}
```

Notice that `WorkspaceLayout` still has:
- The `TabStrip` component (this renders the tab bar)
- All the layout structure
- It just **doesn't** have `<FloatingLayer />` anymore (that moved to WorkspaceShell)

This way:
- ✅ TabStrip renders on Project Editor (inside WorkspaceLayout)
- ✅ FloatingLayer renders globally (inside WorkspaceShell for all pages)
- ✅ Quick Notes works everywhere